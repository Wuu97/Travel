import { normalizeFeedbackEvent, type TravelFeedbackEvent } from "./model";
const key = "travel:feedback-events"; const maximumEvents = 100; const browserStorage = () => typeof window === "undefined" ? undefined : window.localStorage;
export function readFeedbackEvents(): TravelFeedbackEvent[] { try { const value: unknown = JSON.parse(browserStorage()?.getItem(key) || "[]"); return Array.isArray(value) ? value.map(normalizeFeedbackEvent).filter((event): event is TravelFeedbackEvent => Boolean(event)).slice(-maximumEvents) : []; } catch { return []; } }
export function collectFeedbackEvent(event: Omit<TravelFeedbackEvent, "timestamp"> & { timestamp?: string }): TravelFeedbackEvent | undefined { const normalized = normalizeFeedbackEvent({ ...event, timestamp: event.timestamp ?? new Date().toISOString() }); if (!normalized) return undefined; try { browserStorage()?.setItem(key, JSON.stringify([...readFeedbackEvents(), normalized].slice(-maximumEvents))); } catch { /* Storage is optional. */ } return normalized; }
